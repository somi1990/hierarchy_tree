/// Which direction a [HierarchyTreeView] lays its tree out in.
enum TreeOrientation {
  /// Root on the left, children extend to the right (the original layout).
  leftToRight,

  /// Root on top, children extend downward (classic org-chart layout).
  topToBottom,
}
